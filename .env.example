# FFmpeg API Service Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# Server Configuration
# =============================================================================

# Port for the API server (default: 3000)
PORT=3000

# API Key for authentication (leave empty to disable authentication in dev mode)
# Generate a secure key: openssl rand -hex 32
API_KEY=your-secret-api-key-here

# Base URL for download links (auto-detected from request if not set)
# Set this in production to your public URL
# Example: https://ffmpeg-api.your-domain.com
BASE_URL=

# =============================================================================
# Processing Limits
# =============================================================================

# Maximum number of videos that can be assembled in one request (default: 20)
MAX_VIDEOS=20

# Maximum file size per video in MB (default: 500)
MAX_FILE_SIZE_MB=500

# FFmpeg processing timeout in milliseconds (default: 300000 = 5 minutes)
FFMPEG_TIMEOUT=300000

# Output directory for assembled videos (default: /tmp/ffmpeg-outputs)
OUTPUT_DIR=/tmp/ffmpeg-outputs
