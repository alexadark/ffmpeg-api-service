# FFmpeg API Service Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# Server Configuration
# =============================================================================

# Port for the API server (default: 3000)
PORT=3000

# API Key for authentication (leave empty to disable authentication)
# Generate a secure key: openssl rand -hex 32
API_KEY=your-secret-api-key-here

# =============================================================================
# Supabase Storage Configuration (Required)
# =============================================================================

# Your Supabase project URL
# Example: https://abcdefghijklmnop.supabase.co
SUPABASE_URL=https://your-project.supabase.co

# Supabase service role key (NOT the anon key!)
# Found in: Project Settings > API > service_role key
SUPABASE_SERVICE_KEY=your-service-role-key-here

# Storage bucket name for assembled videos (default: final-reels)
SUPABASE_BUCKET=final-reels

# =============================================================================
# Processing Limits
# =============================================================================

# Maximum number of videos that can be assembled in one request (default: 20)
MAX_VIDEOS=20

# Maximum file size per video in MB (default: 500)
MAX_FILE_SIZE_MB=500

# FFmpeg processing timeout in milliseconds (default: 300000 = 5 minutes)
FFMPEG_TIMEOUT=300000
